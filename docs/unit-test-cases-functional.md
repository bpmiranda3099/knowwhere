| Test Case Scenario ID | Test Case Scenario | Action | Actual Input | Expected Result |
|---|---|---|---|---|
| KN-F-UT-01 | API boots and health responds | Start the API, then open the health page | **URL**: `http://localhost:3000/health` | `/health` responds `200` with JSON containing `status:"ok"` and a `services` object |
| KN-F-UT-02 | Health endpoint returns ready | Open the ready page | **URL**: `http://localhost:3000/ready` | `/ready` responds `200` with JSON `{"status":"ready"}` |
| KN-F-UT-03 | Health endpoint supports selecting services | Open health with a single requested service | **URL**: `http://localhost:3000/health?services=api` | Response includes `services.api:"ok"` and leaves `services.db:"unknown"`, `services.embedding:"unknown"`, `services.reranker:"unknown"`, `services.web:"unknown"` |
| KN-F-UT-04 | Health endpoint reports DB down | Stop the DB service, then open health for DB only | **URL**: `http://localhost:3000/health?services=db` | Response includes `services.db:"error"` |
| KN-F-UT-05 | Health endpoint reports embedding down | Stop the embedding service (or point it to a wrong URL), then open health for embedding only | **Env**: `EMBEDDING_ENDPOINT=http://127.0.0.1:9999/embed` **URL**: `http://localhost:3000/health?services=embedding` | Response includes `services.embedding:"error"` |
| KN-F-UT-06 | Health endpoint reports reranker down | Stop the reranker service (or point it to a wrong URL), then open health for reranker only | **Env**: `RERANK_ENDPOINT=http://127.0.0.1:9998/rerank` **URL**: `http://localhost:3000/health?services=reranker` | Response includes `services.reranker:"error"` |
| KN-F-UT-07 | Health endpoint defaults when no services specified | Open health with no query string | **URL**: `http://localhost:3000/health` | Response includes `services.api:"ok"`; `services.web` remains `"unknown"` |
| KN-F-UT-08 | Search endpoint returns results payload | Send a valid search request | **Request**: `POST http://localhost:3000/search` **Headers**: `Content-Type: application/json` **Body**: `{"q":"traffic control","mode":"lexical","level":"paper","limit":5}` | Response is `200` and includes fields `results` (array), `mode`, and `level` |
| KN-F-UT-09 | Search endpoint rejects invalid body | Send an empty search body | **Request**: `POST http://localhost:3000/search` **Headers**: `Content-Type: application/json` **Body**: `{}` | Response is `400` with a validation error payload |
| KN-F-UT-10 | Search endpoint rejects invalid year range | Send a search with `yearFrom > yearTo` | **Request**: `POST http://localhost:3000/search` **Headers**: `Content-Type: application/json` **Body**: `{"q":"hello","filters":{"yearFrom":2025,"yearTo":2020}}` | Response is `400` with `{ "error": "yearFrom cannot be greater than yearTo" }` |
| KN-F-UT-11 | Ingest endpoint starts a job | Start an ingest job | **Request**: `POST http://localhost:3000/ingest` **Headers**: `Content-Type: application/json` **Body**: `{"source":"openalex","query":"machine learning","count":5}` | Response is `202` with JSON containing `message:"Ingest started"`, `source`, `query`, `count`, and a numeric `pid` |
| KN-F-UT-12 | Ingest endpoint rejects invalid body | Send an empty ingest body | **Request**: `POST http://localhost:3000/ingest` **Headers**: `Content-Type: application/json` **Body**: `{}` | Response is `400` with an error payload describing the invalid fields |
| KN-F-UT-13 | Ingest endpoint fails when ingest script missing | Remove/rename the script, then start ingest | **Pre-step**: rename `dist/scripts/ingest/ingestArxiv.js` → `ingestArxiv.js.bak` **Request**: `POST http://localhost:3000/ingest` **Body**: `{"source":"arxiv","query":"test","count":1}` | Response is `500` with error text like `Ingest script not found: ingestArxiv.js` |
| KN-F-UT-14 | Contact endpoint returns 501 when SMTP not configured | Ensure SMTP is unset and submit contact | **Env**: `SMTP_HOST=""`, `SMTP_USER=""`, `SMTP_PASS=""` **Request**: `POST http://localhost:3000/contact` **Body**: `{"name":"Ada","email":"ada@example.com","role":"Student","message":"Hello"}` | Response is `501` with `{ "error": "contact_not_configured" }` |
| KN-F-UT-15 | Contact endpoint rejects invalid body | Submit contact with invalid fields | **Request**: `POST http://localhost:3000/contact` **Body**: `{"name":"","email":"bad","role":"","message":""}` | Response is `400` with `{ "error": "invalid_body", ... }` |
| KN-F-UT-16 | Contact endpoint handles admin email failure | Configure SMTP with wrong password, then submit contact | **Env**: `SMTP_HOST=<real host>`, `SMTP_USER=<real user>`, `SMTP_PASS=<wrong pass>` **Request**: `POST http://localhost:3000/contact` **Body**: `{"name":"Ada","email":"ada@example.com","role":"Student","message":"Hello"}` | Response is `502` with `{ "error":"smtp_send_failed", "stage":"admin", ... }` |
| KN-F-UT-17 | Contact endpoint handles confirmation email failure | Make the second email fail after the first succeeds | **Setup**: allow first send, block second (SMTP rule / recipient reject) **Request**: `POST http://localhost:3000/contact` **Body**: `{"name":"Ada","email":"ada@example.com","role":"Student","message":"Hello"}` | Response is `502` with `{ "error":"smtp_send_failed", "stage":"confirm", ... }` |
| KN-F-UT-18 | Contact endpoint escapes HTML and formats newlines | Submit contact with HTML + newline in message | **Request**: `POST http://localhost:3000/contact` **Body**: `{"name":"Ada","email":"ada@example.com","role":"Student","message":"<script>\\nX</script>"}` | Sent HTML contains escaped content like `&lt;script&gt;<br/>X&lt;/script&gt;` and response is `{ "ok": true }` |
| KN-F-UT-19 | Contact endpoint works without a logo file | Remove logo file and submit contact | **Pre-step**: ensure `/app/assets/logo.png` missing/unreadable **Request**: `POST http://localhost:3000/contact` **Body**: `{"name":"Ada","email":"ada@example.com","role":"Student","message":"Hello"}` | Response is `{ "ok": true }` and emails are sent without a logo attachment |
| KN-F-UT-20 | API key guard allows requests when API key unset | Run search with no API key configured | **Env**: `API_KEY=""` **Request**: `POST http://localhost:3000/search` **Body**: `{"q":"hello","mode":"lexical","level":"paper"}` | Request is not blocked (no `401`) |
| KN-F-UT-21 | API key guard bypasses health | Request health without API key header | **Env**: `API_KEY="secret123"` **URL**: `http://localhost:3000/health` | Request still succeeds (no `401`) |
| KN-F-UT-22 | API key guard bypasses ready | Request ready without API key header | **Env**: `API_KEY="secret123"` **URL**: `http://localhost:3000/ready` | Request still succeeds (no `401`) |
| KN-F-UT-23 | API key guard bypasses contact | Submit contact without API key header | **Env**: `API_KEY="secret123"` **Request**: `POST http://localhost:3000/contact` **Body**: `{"name":"Ada","email":"ada@example.com","role":"Student","message":"Hello"}` | Request is not rejected by the API-key check (it may still fail for SMTP) |
| KN-F-UT-24 | API key guard rejects missing/wrong key | Submit protected request without/with wrong API key | **Env**: `API_KEY="secret123"` **Request**: `POST http://localhost:3000/search` **Body**: `{"q":"hello","mode":"lexical","level":"paper"}` **Try #1 headers**: none **Try #2 headers**: `x-api-key: wrong` | Both requests return `401` with `{"error":"unauthorized"}` |
| KN-F-UT-25 | DB helper forwards SQL and params | Run a small Node script calling `query(text, params)` | **Script call**: `query("SELECT 1", [])` | The DB layer receives `SELECT 1` and `[]` unchanged |
| KN-F-UT-26 | DB helper returns a connected client | Run a small Node script calling `getClient()` | **Script call**: `getClient()` | A client object is returned (connection acquired) |
| KN-F-UT-27 | DB helper closes the pool | Run a small Node script calling `closePool()` | **Script call**: `closePool()` | The pool close call completes without throwing |
| KN-F-UT-28 | Embedding client reads vector from `embeddings` shape | Call `embedText` against a test endpoint | **Env**: `EMBEDDING_ENDPOINT=<test server>` returning `{"embeddings":[[1,2,3]]}` **Call**: `embedText("hi")` | Returned vector is `[1,2,3]` |
| KN-F-UT-29 | Embedding client reads vector from `data[0].embedding` | Call `embedText` against a test endpoint | **Env**: `EMBEDDING_ENDPOINT=<test server>` returning `{"data":[{"embedding":[4,5]}]}` **Call**: `embedText("hi")` | Returned vector is `[4,5]` |
| KN-F-UT-30 | Embedding client reads vector from `vector` shape | Call `embedText` against a test endpoint | **Env**: `EMBEDDING_ENDPOINT=<test server>` returning `{"vector":[9]}` **Call**: `embedText("hi")` | Returned vector is `[9]` |
| KN-F-UT-31 | Embedding client errors on unsupported JSON | Call `embedText` against a test endpoint | **Env**: `EMBEDDING_ENDPOINT=<test server>` returning `{"nope":true}` **Call**: `embedText("hi")` | Error includes text like `Unsupported embedding response shape` |
| KN-F-UT-32 | Embedding client includes HTTP status in errors | Call `embedText` against a failing endpoint | **Env**: `EMBEDDING_ENDPOINT=<test server>` responding `502` with text `bad gateway` **Call**: `embedText("hi")` | Error text includes `embedText failed (502): bad gateway` |
| KN-F-UT-33 | Rerank throws when endpoint missing | Call `rerank` without endpoint configured | **Env**: `RERANK_ENDPOINT` unset **Call**: `rerank("q", ["a"])` | Error contains `RERANK_ENDPOINT is required` |
| KN-F-UT-34 | Rerank returns null when disabled (SKIP_RERANK=1) | Call `rerank` in test mode with skip enabled | **Env**: `NODE_ENV=test`, `SKIP_RERANK=1`, `RERANK_ENDPOINT=http://rerank` **Call**: `rerank("q", ["a"])` | Result is `null` |
| KN-F-UT-35 | Rerank returns null when disabled (SKIP_RERANK=true) | Call `rerank` in test mode with skip enabled | **Env**: `NODE_ENV=test`, `SKIP_RERANK=true`, `RERANK_ENDPOINT=http://rerank` **Call**: `rerank("q", ["a"])` | Result is `null` |
| KN-F-UT-36 | Rerank treats SKIP_RERANK=0 as not disabled | Call `rerank` with empty candidates | **Env**: `NODE_ENV=test`, `SKIP_RERANK=0`, `RERANK_ENDPOINT=http://rerank` **Call**: `rerank("q", [])` | Result is `null` only because candidates are empty |
| KN-F-UT-37 | Rerank returns null when candidates empty | Call `rerank` with no candidates | **Call**: `rerank("q", [])` | Result is `null` |
| KN-F-UT-38 | Rerank returns scores on valid response | Call `rerank` against a test endpoint | **Env**: `RERANK_ENDPOINT=<test server>` returning `{"scores":[0.2,0.1]}` **Call**: `rerank("q", ["a","b"])` | Returned value is `[0.2,0.1]` |
| KN-F-UT-39 | Rerank throws on non-OK response | Call `rerank` against a failing endpoint | **Env**: `RERANK_ENDPOINT=<test server>` returning status `500` and text `boom` **Call**: `rerank("q", ["a"])` | Error includes text like `rerank failed (500)` |
| KN-F-UT-40 | Rerank returns null when response has no scores | Call `rerank` against a test endpoint | **Env**: `RERANK_ENDPOINT=<test server>` returning `{}` **Call**: `rerank("q", ["a"])` | Result is `null` |
| KN-F-UT-41 | Rerank wraps fetch exceptions | Stop reranker, then call `rerank` | **Env**: `RERANK_ENDPOINT=http://127.0.0.1:9998/rerank` **Call**: `rerank("q", ["a"])` | Error includes text like `rerank error:` and the failure message |
| KN-F-UT-42 | Search service lexical mode skips embeddings | Send lexical search | **Request**: `POST http://localhost:3000/search` **Body**: `{"q":"hello","mode":"lexical","level":"paper","limit":5}` | Results are returned without requiring the embedding/rerank services |
| KN-F-UT-43 | Search service hybrid mode uses embeddings and rerank | Send hybrid search | **Request**: `POST http://localhost:3000/search` **Body**: `{"q":"hello","mode":"hybrid","level":"paper","limit":5}` | When services are available, embeddings/rerank are used and results are ranked |
| KN-F-UT-44 | Search service removes duplicate titles | Query a dataset containing duplicate titles | **Dataset**: 2 papers titled “Experimentation in Software Engineering” **Request**: `{"q":"experimentation","mode":"hybrid","level":"paper","limit":10}` | Only one of the duplicate-title results remains (the stronger one) |
| KN-F-UT-45 | Search service penalizes “stub” metadata rows | Query a dataset containing a stub row | **Dataset**: one row where `abstract==title` and one substantive row **Request**: `{"q":"traffic","mode":"hybrid","level":"paper","limit":10}` | The substantive-abstract result ranks above the stub row |
| KN-F-UT-46 | chunking creates overlap | Chunk sample text with overlap | **Input text**: 50 words `w0 ... w49` **Params**: `chunkSize=10`, `overlap=5` | Second chunk starts with the 6th word of the first chunk |
| KN-F-UT-47 | PDF text extraction returns null on download failure | Try extracting text from a missing PDF | **URL**: `http://example.invalid/test.pdf` (or a 404 URL) | Result is `null` |
| KN-F-UT-48 | PDF text extraction returns trimmed text on success | Try extracting text from a valid PDF | **URL**: any reachable 1–2 page PDF URL | Returned text has no leading/trailing whitespace |
| KN-F-UT-49 | PDF text extraction returns null on parse failure | Try extracting text from a corrupt/non-PDF | **URL**: a URL that returns non-PDF content but ends with `.pdf` | Result is `null` |
| KN-F-UT-50 | Ingest DB helper returns null for missing venue | Call `ensureVenue` with missing name | **Call**: `ensureVenue(client, null)` and `ensureVenue(client, undefined)` | Result is `null` and no venue insert occurs |
| KN-F-UT-51 | Ingest DB helper returns existing source id | Call `ensureSource` where source exists | **Call**: `ensureSource(client, "arxiv")` with DB returning existing row | Existing source ID is returned |
| KN-F-UT-52 | Ingest DB helper creates subject when missing | Call `ensureSubject` where subject missing | **Call**: `ensureSubject(client, "cs.CL")` with DB returning no row then insert row | A new subject ID is returned |
| KN-F-UT-53 | Ingest DB helper creates author when missing | Call `ensureAuthor` where author missing | **Call**: `ensureAuthor(client, "Ada")` with DB returning no row then insert row | A new author ID is returned |
| KN-F-UT-54 | Ingest DB helper links paper to subject and author | Call link helpers for a paper | **Calls**: `linkPaperSubject(client, "p1", 1)`, `linkPaperAuthor(client, "p1", 2, 0)` | Link queries execute successfully |
| KN-F-UT-55 | Interactive ingest summary shows pacing | Build a summary string | **Input**: `source="arxiv"`, `query="machine learning"`, `quantity=25`, `pacingMs=3000` | Summary mentions `arXiv` and includes `1 request every 3.0s` |
| KN-F-UT-56 | Interactive ingest dispatches selected runner | Run selected ingest for Crossref | **Input**: `source="crossref"`, `query="nlp"`, `quantity=10`, `pacingMs=1000` | Only the Crossref runner is called with those values |
| KN-F-UT-57 | Ingest CLIs do not auto-run on import | Import module without calling any function | **REPL**: `require("./dist/scripts/ingest/ingestOpenAlex.js")` | No network request runs automatically just from importing |
