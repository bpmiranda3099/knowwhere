| Test Case Scenario ID | Test Case Scenario | Action | Actual Input | Expected Result |
|---|---|---|---|---|
| KN-F-IT-01 | API + database health check works | Start API + DB, then open the health page that checks both | **Pre-step**: start services (example: `docker compose up -d db api`) **Then open**: `http://localhost:3000/health?services=api,db` | Response is `200` with `status:"ok"` and `services.api:"ok"`; `services.db` is present (typically `ok` when DB is reachable) |
| KN-F-IT-02 | Stats endpoint returns numeric counts | Start API + DB, then open the stats page | **Pre-step**: start services (example: `docker compose up -d db api`) **Then open**: `http://localhost:3000/stats` | Response is `200` and includes numeric-ish counts for `papers` and `chunks` (can be parsed as numbers \(\ge 0\)) |
| KN-F-IT-03 | Lexical search returns a results array | Start API + DB, then submit a lexical search | **Pre-step**: start services (example: `docker compose up -d db api`) **Then send**: `POST http://localhost:3000/search` with JSON body `{ "q": "test", "mode": "lexical", "level": "paper", "limit": 5 }` | Response is `200` and contains `results` as an array (may be empty depending on DB contents) |
| KN-F-IT-04 | Logs endpoint returns a logs array | Start API + DB, then open the logs page | **Pre-step**: start services (example: `docker compose up -d db api`) **Then open**: `http://localhost:3000/logs?limit=5` | Response is `200` and contains `logs` as an array |

| KN-F-IT-05 | Embedding service is reachable from the API (health) | Start the full stack, then check embedding health via the API health page | **Pre-step**: start containers (example: `docker compose up -d db embedding api`) **Then open**: `http://localhost:3000/health?services=embedding` | The page responds successfully and shows embedding as available/ok |
| KN-F-IT-06 | Reranker service is reachable from the API (health) | Start the full stack, then check reranker health via the API health page | **Pre-step**: start containers (example: `docker compose up -d db reranker api`) **Then open**: `http://localhost:3000/health?services=reranker` | The page responds successfully and shows reranker as available/ok |
| KN-F-IT-07 | Embed endpoint works with the real embedding service | Start the stack with embedding, then send an embed request | **Pre-step**: `docker compose up -d db embedding api` **Then in an API client**: send to `http://localhost:3000/embed` with body `{ "texts": ["hello world"] }` | You receive a successful response containing embeddings (a vector for each input text) |
| KN-F-IT-08 | Rerank endpoint works with the real reranker service | Start the stack with reranker, then send a rerank request | **Pre-step**: `docker compose up -d db reranker api` **Then in an API client**: send to `http://localhost:3000/rerank` with body `{ "query": "machine learning", "documents": ["doc a", "doc b"] }` | You receive a successful response containing scores for each document |
| KN-F-IT-09 | Semantic or hybrid search works end-to-end (DB + embedding, optional rerank) | Start the stack with DB + embedding (and reranker if you use it), then run a semantic/hybrid search | **Pre-step**: `docker compose up -d db embedding api` (optional: also `reranker`) **Then in an API client**: send to `http://localhost:3000/search` with body like `{ "q": "graph neural networks", "mode": "semantic", "level": "paper", "limit": 5 }` | The request succeeds and returns a response with a `results` array (results may vary by dataset, but it should not error) |
| KN-F-IT-10 | Web reverse-proxy to API works (`/api/*`) | Start the web + api containers, then open the proxied API health endpoint from the web host | **Pre-step**: `docker compose up -d web api` **Then open**: `http://localhost:8080/api/health?services=api` | The request succeeds, proving the web reverse-proxy routes `/api/*` to the API |
| KN-F-IT-11 | Web “Contact” submit path reaches the API (expected 501 when not configured) | Start web + api without SMTP configured, then submit the contact form | **Pre-step**: ensure SMTP is not configured (no SMTP env vars), start `docker compose up -d web api` **Then**: open `http://localhost:8080/web/index.html`, go to Contact, fill the form, click Send | The UI shows an error (since SMTP is not configured) but the site remains usable and the request path is exercised end-to-end through the proxy |
| KN-F-IT-12 | Ingest route correctly reports “script missing” when build output is absent | Start the API without built ingest scripts and submit ingest | **Pre-step**: run API in a way where `dist/scripts/ingest/*.js` are not present **Then in an API client**: send to `http://localhost:3000/ingest` with body `{ "source": "arxiv", "query": "cat:cs.CL", "count": 1 }` | The API responds with an error indicating the ingest script is not found |
| KN-F-IT-13 | Ingest route accepts a request when built scripts are present | Build the project, start the API with built scripts, then submit ingest | **Pre-step**: build to produce `dist/scripts/ingest/*.js` and start API **Then in an API client**: send to `http://localhost:3000/ingest` with body `{ "source": "arxiv", "query": "cat:cs.CL", "count": 1 }` | The API accepts the request and returns an “accepted/started” response including a PID |

