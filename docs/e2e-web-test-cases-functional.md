| Test Case Scenario ID | Test Case Scenario | Action | Actual Input | Expected Result |
|---|---|---|---|---|
| KN-F-UAT-01 | Web home page loads and can open API documentation | Open the KnowWhere website, then click “API” in the top menu | **Website URL**: the KnowWhere site (example: `http://localhost:8080/web/index.html`) | The home page loads and the API documentation page opens |
| KN-F-UAT-02 | Web search page loads and shows search controls | Open the KnowWhere Search page | **Website URL**: the KnowWhere Search page (example: `http://localhost:8080/web/test.html`) | The search page loads and you can see a search box, a Search button, and an area where results will appear |
| KN-F-UAT-03 | Web search can run a query and show results | Type a search query and click Search | **Query text**: `graph neural networks` | The search completes without showing an error message and at least one result appears |
| KN-F-UAT-04 | Web search shows two result columns (Standard vs KnowWhere) | Open the Search page and observe the results area | **None** | Two columns are shown: `Standard Search` and `KnowWhere Search` |
| KN-F-UAT-05 | Web search shows a “Standard Search” results area | Perform a search and view the Standard Search column | **Query text**: `machine learning` | Standard Search results area shows items (or a clear “no results” state if dataset is empty) |
| KN-F-UAT-06 | Web search shows a “KnowWhere Search” results area | Perform a search and view the KnowWhere Search column | **Query text**: `machine learning` | KnowWhere Search results area shows items (or a clear “no results” state if dataset is empty) |
| KN-F-UAT-07 | Web search supports pagination controls | Perform a search that returns more than one page of results | **Query text**: any query that yields many results in your dataset | Pagination controls appear (top and/or bottom) and you can move between pages |
| KN-F-UAT-08 | Web search accepts a long query without breaking the UI | Paste a long query and click Search | **Query text (long)**: `I am looking for research papers about using graph neural networks for molecular property prediction, including message passing neural networks, attention-based GNNs, benchmarks like QM9, and comparisons to transformer models` | The page remains responsive, the query stays visible/scrollable in the input, and results (or a clear “no results” state) appear without showing an error message |
| KN-F-UAT-09 | Web search tolerates a misspelled query and still completes | Type a misspelled query and click Search | **Query text (misspelled)**: `grph nueral netwroks moleculs` | The search completes without the page showing an error message; results may vary, but the UI should not break |
| KN-F-UAT-10 | Home page “Get Started” button opens the Search page | On the home page, click “Get Started” | **Website URL**: Home page | You are taken to the Search page |
| KN-F-UAT-11 | Home page top-menu “Get Started” opens the Search page | On the home page, click “Get Started” in the top menu | **Website URL**: Home page | You are taken to the Search page |
| KN-F-UAT-12 | Home page menu can jump to “Features” | On the home page, click “Features” in the top menu | **Menu item**: `Features` | The page scrolls/jumps to the Features section |
| KN-F-UAT-13 | Home page menu can jump to “About” | On the home page, click “About” in the top menu | **Menu item**: `About` | The page scrolls/jumps to the About section |
| KN-F-UAT-14 | Home page menu can jump to “Contact” | On the home page, click “Contact” in the top menu | **Menu item**: `Contact` | The page scrolls/jumps to the Contact section and the contact form is visible |
| KN-F-UAT-15 | Contact form blocks sending when required fields are empty | Go to Contact and click “Send Message” without filling the form | **Form fields**: leave Name/Email/Message empty | A message appears telling you to fill out all fields, and the message is not sent |
| KN-F-UAT-16 | Contact form accepts leading/trailing spaces in Name and Message | Fill out the form using extra spaces and submit | **Name**: `  Ada  ` **Email**: `ada@example.com` **Role**: `Student` **Message**: `  Hello there  ` | The message is accepted (or fails only for service reasons), and the UI does not show a “fill out all fields” error for whitespace-padded values |
| KN-F-UAT-17 | Contact form blocks sending when Email format is invalid | Enter an invalid email and try to send | **Name**: `Ada` **Email**: `ada@` **Role**: `Student` **Message**: `Hello` | The form blocks sending and shows a validation error (browser validation or an on-page message) |
| KN-F-UAT-18 | Contact form enforces the message length limit | Type a message longer than the limit and observe the counter | **Message**: type until past 250 characters | You cannot exceed the max length; the counter reflects the limit and the message stays at the maximum allowed length |
| KN-F-UAT-19 | Contact form role can be changed and is accepted | Choose a different role and submit | **Role**: `Faculty` (or any other option) with valid Name/Email/Message | The role selection is accepted and the message is sent (or fails only for service reasons) |
| KN-F-UAT-20 | Contact form can send a message successfully | Fill out the contact form and submit | **Name**: `Ada` **Email**: `ada@example.com` **Role**: `Student` **Message**: `Hello` | A success message is shown and the form clears (the message is accepted for delivery) |
| KN-F-UAT-21 | Contact form shows an error if sending fails | Fill out the contact form and submit when the service is unavailable/misconfigured | **Form fields**: valid Name/Email/Role/Message | An error message is shown telling you the message failed to send and you can try again |
| KN-F-UAT-22 | API documentation page loads and shows the left navigation menu | Open the API documentation page | **Website URL**: API documentation page (example: `http://localhost:8080/web/api.html`) | The API documentation page loads and a left-side menu is visible with sections like API Documentation, Auth & Rate Limits, Search, Ingest, Embed, Rerank, Health, Stats, Logs, Model Services, Error Codes, and Search |
| KN-F-UAT-23 | API documentation menu items jump to their section | On the API documentation page, click a menu item (example: “Auth & Rate Limits”) | **Menu item**: `Auth & Rate Limits` | The page jumps/scrolls to the matching section and its heading is visible |
| KN-F-UAT-24 | API documentation “Search” menu item opens the web search page | On the API documentation page, click “Search” in the left menu | **Menu item**: `Search` | The browser opens the KnowWhere Search page |
| KN-F-UAT-25 | API documentation code examples can switch languages (Ingest) | In the Ingest section, switch the code example tab between cURL / Node.js / Python | **Tabs**: `cURL`, `Node.js`, `Python` | The displayed code example changes when you click each tab |
| KN-F-UAT-26 | API documentation code examples can switch languages (Search) | In the Search section, switch the code example tab between cURL / Node.js / Python | **Tabs**: `cURL`, `Node.js`, `Python` | The displayed code example changes when you click each tab |
| KN-F-UAT-27 | API documentation code examples can switch languages (Health) | In the Health section, switch the code example tab between cURL / Node.js / Python | **Tabs**: `cURL`, `Node.js`, `Python` | The displayed code example changes when you click each tab |

